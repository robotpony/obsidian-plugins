{
  "name": "@warped-command/shared",
  "version": "1.0.0",
  "description": "Warped Command's own copy of the shared Warped-plugins utilities. Duplicated from the root shared/ at the point of the repo split rather than kept as a package/submodule dependency — drifts independently now.",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "devDependencies": {}
}
